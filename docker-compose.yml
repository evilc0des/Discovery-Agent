version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./sessions:/app/sessions
    environment:
      - NODE_ENV=production
      - SESSIONS_DIR=/app/sessions
      # Supabase backend (uncomment and set these when using STORAGE_BACKEND=supabase)
      - STORAGE_BACKEND=supabase
      - SUPABASE_URL=https://yevtdkyyqorpcfezxrso.supabase.co
      - SUPABASE_PUBLISHABLE_KEY=sb_publishable_YB-1oYihhDnycdM42NIARw_hVme0xz0
      - SUPABASE_STORAGE_BUCKET=client-uploads
